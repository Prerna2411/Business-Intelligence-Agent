# Tools

This folder contains small utility scripts for local maintenance.

- `rebuild_rag_index.py`: Re-index files already present in `data/uploads`.
- `inspect_rag_store.py`: Print indexed documents from the persistent RAG stores.
